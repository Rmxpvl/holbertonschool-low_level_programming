#include <stdio.h>
#include "main.h"

/**
 * _isdigit - digit numbers
 * @c: c is the checked number
 * Return: 1 if uppercase, 0 if not
 */

int _isdigit(int c)
{
	if (c >= '0' && c <= '9')
	return (1);


	return (0);
}
