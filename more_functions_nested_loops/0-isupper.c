#include <stdio.h>
#include "main.h"

/**
 * _isupper - Recognize uppercases
 * @c: c is the checked letter
 * Return: 1 if uppercase, 0 if not
 */

int _isupper(int c)
{
	if (c >= 'A' && c <= 'Z')
	return (1);


	return (0);
}
