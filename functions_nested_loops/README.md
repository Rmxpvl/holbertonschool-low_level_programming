# Functions and Nested Loops

This project introduces writing and calling functions in C, using header files, and working with nested loops.

## Files

| File | Description |
|------|-------------|
| `main.h` | Header file with all function prototypes |
| `0-putchar.c` | Program that prints `_putchar` using `_putchar` |
| `1-alphabet.c` | Function that prints the lowercase alphabet |
| `2-print_alphabet_x10.c` | Function that prints the lowercase alphabet 10 times |
| `3-islower.c` | Function that checks if a character is lowercase |
| `4-isalpha.c` | Function that checks if a character is alphabetic |
| `5-sign.c` | Function that prints the sign of a number |
| `6-abs.c` | Function that computes the absolute value of an integer |
| `7-print_last_digit.c` | Function that prints the last digit of a number |
| `8-24_hours.c` | Function that prints every minute of the day starting from 00:00 to 23:59 |
| `9-times_table.c` | Function that prints the 9 times table |
| `10-add.c` | Function that adds two integers and returns the result |
| `11-print_to_98.c` | Function that prints all numbers from `n` to 98 |

## Concepts Covered

- Defining and calling functions in C
- Function prototypes and header files (`#ifndef` guards)
- Nested `for` and `while` loops
- Character and integer arithmetic
- Using `_putchar` as a low-level output primitive

## Requirements

- All C files are compiled with `gcc -Wall -Werror -Wextra -pedantic`
- Programs are compiled on Ubuntu 20.04 LTS
- Functions use the prototype declared in `main.h`

## Author

Holberton School – Low Level Programming
